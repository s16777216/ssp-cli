## 1. 核心 API 實作

- [ ] 1.1 在 `sspApi.js` 新增 `createFolder(remotePath, options)` 方法，使用 WebDAV MKCOL 協定
- [ ] 1.2 實作遞迴建立邏輯 (`-p` 旗標)，逐層建立父目錄
- [ ] 1.3 實作錯誤處理：資料夾已存在、父目錄不存在、權限不足等情境

## 2. CLI 指令實作

- [ ] 2.1 在 `cli.js` 新增 `mkdir` 指令，參數為 `<remote-path>`
- [ ] 2.2 實作 `-p` / `--parents` 旗標支援
- [ ] 2.3 實作路徑編碼處理（中文、空白、特殊字元）
- [ ] 2.4 實作建立成功/失敗訊息顯示

## 3. 測試與驗證

- [ ] 3.1 執行 `node src/test.js` 確認模組載入正常
- [ ] 3.2 測試建立資料夾功能（建立單層、多層資料夾）
- [ ] 3.3 測試錯誤情境（已存在、父目錄不存在、權限不足）