## 1. 核心 API 實作

- [ ] 1.1 在 `sspApi.js` 新增 `moveFile(src, dst, options)` 方法，使用 WebDAV MOVE 協定
- [ ] 1.2 實作目標存在檢查與覆蓋邏輯
- [ ] 1.3 實作錯誤處理：來源不存在、目標已存在、權限不足

## 2. CLI 指令實作

- [ ] 2.1 在 `cli.js` 新增 `mv` 指令，參數為 `<src> <dst>`
- [ ] 2.2 實作 `-i` / `--interactive` 旗標
- [ ] 2.3 實作目標路徑判斷（檔案 vs 目錄）
- [ ] 2.4 實作移動成功/失敗訊息顯示

## 3. 測試與驗證

- [ ] 3.1 執行 `node src/test.js` 確認模組載入正常
- [ ] 3.2 測試移動/重命名功能
- [ ] 3.3 測試錯誤情境（已存在、來源不存在、權限不足）