## 1. 核心 API 實作

- [x] 1.1 在 `sspApi.js` 新增 `moveFile(src, dst, options)` 方法，使用 WebDAV MOVE 協定
- [x] 1.2 實作目標存在檢查與覆蓋邏輯（`412` 反推目標存在；確認後重發 MOVE + `Overwrite: T`）
- [x] 1.3 實作錯誤處理：來源不存在、目標已存在、權限不足、跨儲存空間

## 2. CLI 指令實作

- [x] 2.1 在 `cli.js` 新增 `mv` 指令，參數為 `<src> <dst>`
- [x] 2.2 實作 `-y` / `--yes` 旗標；預設目標已存在時以 readline 互動提示 `目標已存在，覆蓋？ [y/N]`
- [x] 2.3 實作移動成功/失敗訊息顯示

## 3. 測試與驗證

- [x] 3.1 執行 `npm test` 確認模組載入正常
- [x] 3.2 測試移動/重命名功能
- [x] 3.3 測試錯誤情境（已存在、來源不存在、權限不足、跨儲存空間）